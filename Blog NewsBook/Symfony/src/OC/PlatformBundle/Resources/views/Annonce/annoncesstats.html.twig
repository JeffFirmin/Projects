{# src/OC/PlatformBundle/Resources/views/Annonce/index.html.twig #}

{#
{% include '@FOSComment/Thread/async.html.twig' with {'id': 'foo'} %}
<!-- CSS -->
{% stylesheets '@FOSCommentBundle/Resources/public/css/comments.css' %}
<link rel="stylesheet" href="{{ asset_url }}" type="text/css" />
{% endstylesheets %}   #}

{% extends "OCPlatformBundle::layout.html.twig" %}

{% block title %}
  Accueil - {{ parent() }}
{% endblock %}

{% block ocplatform_body %}

  <h2>Statistiques</h2>

{% for result in annonces %}

   {%set annonce = result[0]%}
   {%set annoncesstats = result[1]%}

   il y'a
   {{ result|length }}
   {{annoncesstats}}

  {% else %}
      <li> 0 publication(s)</li>
    {% endfor %}
    

{#    <tr class="active"> 
<td> <a href="{{ path('oc_platform_view', {'id': annonce.id}) }}"> {{ annonce.title }} </a> </td>
<td> le {{ annonce.date|date('d/m/Y') }} </td> 
</td><td> {{annonce.user.username }} </td> 
    {% else %}
      <li>Vous n'avez encore rien publié</li>
    {% endfor %}
 
 </tr>
 </table>
}#

{#
  <ul>
    {% for annonce in annonces %}
      <li>
        <a href="{{ path('oc_platform_view', {'id': annonce.id}) }}">
          {{ annonce.title }}
        </a>
        le {{ annonce.date|date('d/m/Y') }}
      </li>
    {% else %}
      <li>Pas (encore !) d'annonces</li>
    {% endfor %}
  </ul>
#}

{% endblock %}